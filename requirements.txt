# Core database & dependencies
pymongo
requests
nodejs
npm
ansible-core

# Config / secret management
python-dotenv
python-dotenv-vault

# Progress / utilities
tqdm
# numpy>=1.24.0,<2.0.0

# LLM / RAG
langchain
openai
voyageai
langchain-mongodb
langchain-core
langchain-openai
langchain-voyageai

# Infrastructure / automation
ansible-core
# boto3>=1.34.0,<2.0.0
# botocore>=1.34.0,<2.0.0
# awscli>=1.32.0,<2.0.0

# Pin docutils to a compatible version to avoid Sphinx warning
docutils

# Optional: for better notebook outputs and debugging
colorama
rich 
tabulate